[project]
name = "worldcup2026"
version = "0.0.0"
description = "Modello probabilistico per la fase a eliminazione del Mondiale 2026"
requires-python = ">=3.11"

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
